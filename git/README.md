Install the gitconfig (please change the user name in the gitconfig file)

```
ln -s ~/dotfiles/git/gitconfig ~/.gitconfig
```

---

Install the `git super` command.

```
ln -s ~/dotfiles/git/git-super ~/bin/git-super
```
